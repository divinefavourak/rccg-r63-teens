# Where the Django API lives.
#
# Leave unset in development: src/api/config.ts falls back to the machine
# currently serving the Expo bundle, on port 8000 — which is what makes a
# physical phone work without editing anything. Set it for staging/production.
#
# EXPO_PUBLIC_API_URL=https://api.example.org/api/v1
